import 'package:flutter/material.dart';

class PandemicPrayer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Prayer during a Pandemic/Plague"),
        ),
        body: Container(
          color: Colors.amber,
        ));
  }
}
